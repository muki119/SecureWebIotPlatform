package constants

// this holds the constants for the event bus

const MAIL_SERVICE = "MAIL_SERVICE" // the consumer group
// this is what the mailing service is listening to
const(
	AUTH_USER_CREATED = "AUTH_SERVICE.USER_CREATED"
	AUTH_USER_DELETED = "AUTH_SERVICE.USER_DELETED"
)

