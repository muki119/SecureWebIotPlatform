import type { Request, Response, NextFunction } from 'express';


export default function DeleteDomainController(req: Request, res: Response, next: NextFunction) {
    // multi table opperation
    // must check if user is owner 

}